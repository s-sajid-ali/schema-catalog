# schema source: HashiCorp Vault v1.4.1 https://github.com/hashicorp/vault-secrets-operator
apiVersion <string> enum=secrets.hashicorp.com/v1beta1
kind <string> enum=CSISecrets
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# CSISecretsSpec defines the desired state of CSISecrets. It contains the configuration for the CSI driver to populate the secret data.
spec.accessControl <object> (required)	# AccessControl provides configuration for controlling access to the secret.
spec.accessControl.matchPolicy <string> enum=any|all default="all"	# MatchPolicy is the policy to use when matching the access control rules. If set to "any", only one of the rules should match. If set to "all", all the rules should match.
spec.accessControl.namespacePatterns <[]string>	# NamespacePatterns is a list of namespace name regex patterns that are allowed access.
spec.accessControl.podLabels <map[string]string>	# PodLabels is a map of pod label key-value pairs that should be allowed access.
spec.accessControl.podNamePatterns <[]string>	# PodNamePatterns is a list of pod name regex patterns that should be allowed access.
spec.accessControl.serviceAccountPattern <string> (required)	# ServiceAccountPattern is the name of the service account that should be used to access the secret. It can be specified as a regex pattern. A valid service account is always required.
spec.namespace <string>	# Namespace is the Vault namespace where the secret is located.
spec.secrets <object> (required)	# Secrets that will be synced with the CSI driver.
spec.secrets.transformation <object>	# Transformation provides configuration for transforming the secret data before it is stored in the CSI volume.
spec.secrets.transformation.excludeRaw <boolean>	# ExcludeRaw data from the destination Secret. Exclusion policy can be set globally by including 'exclude-raw` in the '--global-transformation-options' command line flag. If set, the command line flag always takes precedence over this configuration.
spec.secrets.transformation.excludes <[]string>	# Excludes contains regex patterns used to filter top-level source secret data fields for exclusion from the final K8s Secret data. These pattern filters are never applied to templated fields as defined in Templates. They are always applied before any inclusion patterns. To exclude all source secret data fields, you can configure the single pattern ".*".
spec.secrets.transformation.includes <[]string>	# Includes contains regex patterns used to filter top-level source secret data fields for inclusion in the final K8s Secret data. These pattern filters are never applied to templated fields as defined in Templates. They are always applied last.
spec.secrets.transformation.templates <map[string]object>	# Templates maps a template name to its Template. Templates are always included in the rendered K8s Secret, and take precedence over templates defined in a SecretTransformation.
spec.secrets.transformation.templates.<key>.name <string>	# Name of the Template
spec.secrets.transformation.templates.<key>.text <string> (required)	# Text contains the Go text template format. The template references attributes from the data structure of the source secret. Refer to https://pkg.go.dev/text/template for more information.
spec.secrets.transformation.transformationRefs <[]object>	# TransformationRefs contain references to template configuration from SecretTransformation.
spec.secrets.transformation.transformationRefs[].ignoreExcludes <boolean>	# IgnoreExcludes controls whether to use the SecretTransformation's Excludes data key filters.
spec.secrets.transformation.transformationRefs[].ignoreIncludes <boolean>	# IgnoreIncludes controls whether to use the SecretTransformation's Includes data key filters.
spec.secrets.transformation.transformationRefs[].name <string> (required)	# Name of the SecretTransformation resource.
spec.secrets.transformation.transformationRefs[].namespace <string>	# Namespace of the SecretTransformation resource.
spec.secrets.transformation.transformationRefs[].templateRefs <[]object>	# TemplateRefs map to a Template found in this TransformationRef. If empty, then all templates from the SecretTransformation will be rendered to the K8s Secret.
spec.secrets.transformation.transformationRefs[].templateRefs[].keyOverride <string>	# KeyOverride to the rendered template in the Destination secret. If Key is empty, then the Key from reference spec will be used. Set this to override the Key set from the reference spec.
spec.secrets.transformation.transformationRefs[].templateRefs[].name <string> (required)	# Name of the Template in SecretTransformationSpec.Templates. the rendered secret data.
spec.secrets.vaultAppRoleSecretIDs <[]object>	# VaultAppRoleSecretIDs is a list of AppRole secret IDs to be used to populate the secret.
spec.secrets.vaultAppRoleSecretIDs[].cidrList <[]string>	# CIDRList is the list of CIDR blocks that access the secret ID.
spec.secrets.vaultAppRoleSecretIDs[].metadata <map[string]string>	# Metadata is the metadata to be associated with the secret ID. It is set on the token generated by the secret ID.
spec.secrets.vaultAppRoleSecretIDs[].mount <string> (required)	# Mount path to the AppRole auth engine.
spec.secrets.vaultAppRoleSecretIDs[].numUses <integer>	# NumUses is the number of times the secret ID can be used.
spec.secrets.vaultAppRoleSecretIDs[].role <string> (required)	# Role is the name of the AppRole.
spec.secrets.vaultAppRoleSecretIDs[].syncRoleID <boolean>	# SyncRoleID is the flag to fetch the role ID from the AppRole auth engine. Requires that the provisioning VaultAuth has the necessary permissions to fetch the role ID.
spec.secrets.vaultAppRoleSecretIDs[].tokenBoundCIDRs <[]string>	# TokenBoundCIDRs is the list of CIDR blocks that can be used to authenticate using tokens generated by this secret ID.
spec.secrets.vaultAppRoleSecretIDs[].transformation <object>	# Transformation provides configuration for transforming the secret data before it is stored in the CSI volume.
spec.secrets.vaultAppRoleSecretIDs[].transformation.excludeRaw <boolean>	# ExcludeRaw data from the destination Secret. Exclusion policy can be set globally by including 'exclude-raw` in the '--global-transformation-options' command line flag. If set, the command line flag always takes precedence over this configuration.
spec.secrets.vaultAppRoleSecretIDs[].transformation.excludes <[]string>	# Excludes contains regex patterns used to filter top-level source secret data fields for exclusion from the final K8s Secret data. These pattern filters are never applied to templated fields as defined in Templates. They are always applied before any inclusion patterns. To exclude all source secret data fields, you can configure the single pattern ".*".
spec.secrets.vaultAppRoleSecretIDs[].transformation.includes <[]string>	# Includes contains regex patterns used to filter top-level source secret data fields for inclusion in the final K8s Secret data. These pattern filters are never applied to templated fields as defined in Templates. They are always applied last.
spec.secrets.vaultAppRoleSecretIDs[].transformation.templates <map[string]object>	# Templates maps a template name to its Template. Templates are always included in the rendered K8s Secret, and take precedence over templates defined in a SecretTransformation.
spec.secrets.vaultAppRoleSecretIDs[].transformation.templates.<key>.name <string>	# Name of the Template
spec.secrets.vaultAppRoleSecretIDs[].transformation.templates.<key>.text <string> (required)	# Text contains the Go text template format. The template references attributes from the data structure of the source secret. Refer to https://pkg.go.dev/text/template for more information.
spec.secrets.vaultAppRoleSecretIDs[].transformation.transformationRefs <[]object>	# TransformationRefs contain references to template configuration from SecretTransformation.
spec.secrets.vaultAppRoleSecretIDs[].transformation.transformationRefs[].ignoreExcludes <boolean>	# IgnoreExcludes controls whether to use the SecretTransformation's Excludes data key filters.
spec.secrets.vaultAppRoleSecretIDs[].transformation.transformationRefs[].ignoreIncludes <boolean>	# IgnoreIncludes controls whether to use the SecretTransformation's Includes data key filters.
spec.secrets.vaultAppRoleSecretIDs[].transformation.transformationRefs[].name <string> (required)	# Name of the SecretTransformation resource.
spec.secrets.vaultAppRoleSecretIDs[].transformation.transformationRefs[].namespace <string>	# Namespace of the SecretTransformation resource.
spec.secrets.vaultAppRoleSecretIDs[].transformation.transformationRefs[].templateRefs <[]object>	# TemplateRefs map to a Template found in this TransformationRef. If empty, then all templates from the SecretTransformation will be rendered to the K8s Secret.
spec.secrets.vaultAppRoleSecretIDs[].transformation.transformationRefs[].templateRefs[].keyOverride <string>	# KeyOverride to the rendered template in the Destination secret. If Key is empty, then the Key from reference spec will be used. Set this to override the Key set from the reference spec.
spec.secrets.vaultAppRoleSecretIDs[].transformation.transformationRefs[].templateRefs[].name <string> (required)	# Name of the Template in SecretTransformationSpec.Templates. the rendered secret data.
spec.secrets.vaultAppRoleSecretIDs[].ttl <string> pattern="^([0-9]+(\\.[0-9]+)?(s|m|h))$"	# TTL is the TTL for the secret ID, after which it becomes invalid.
spec.secrets.vaultAppRoleSecretIDs[].wrapTTL <string> pattern="^([0-9]+(\\.[0-9]+)?(s|m|h))$"	# WrapTTL is the TTL for the wrapped secret ID.
spec.secrets.vaultStaticSecrets <[]object>	# VaultStaticSecrets is a list of static secrets to be synced by the CSI driver.
spec.secrets.vaultStaticSecrets[].mount <string> (required)	# Mount for the secret in Vault
spec.secrets.vaultStaticSecrets[].path <string> (required)	# Path of the secret in Vault, corresponds to the `path` parameter for: kv-v1: https://developer.hashicorp.com/vault/api-docs/secret/kv/kv-v1#read-secret kv-v2: https://developer.hashicorp.com/vault/api-docs/secret/kv/kv-v2#read-secret-version
spec.secrets.vaultStaticSecrets[].transformation <object>	# Transformation provides configuration for transforming the secret data before it is stored in the CSI volume.
spec.secrets.vaultStaticSecrets[].transformation.excludeRaw <boolean>	# ExcludeRaw data from the destination Secret. Exclusion policy can be set globally by including 'exclude-raw` in the '--global-transformation-options' command line flag. If set, the command line flag always takes precedence over this configuration.
spec.secrets.vaultStaticSecrets[].transformation.excludes <[]string>	# Excludes contains regex patterns used to filter top-level source secret data fields for exclusion from the final K8s Secret data. These pattern filters are never applied to templated fields as defined in Templates. They are always applied before any inclusion patterns. To exclude all source secret data fields, you can configure the single pattern ".*".
spec.secrets.vaultStaticSecrets[].transformation.includes <[]string>	# Includes contains regex patterns used to filter top-level source secret data fields for inclusion in the final K8s Secret data. These pattern filters are never applied to templated fields as defined in Templates. They are always applied last.
spec.secrets.vaultStaticSecrets[].transformation.templates <map[string]object>	# Templates maps a template name to its Template. Templates are always included in the rendered K8s Secret, and take precedence over templates defined in a SecretTransformation.
spec.secrets.vaultStaticSecrets[].transformation.templates.<key>.name <string>	# Name of the Template
spec.secrets.vaultStaticSecrets[].transformation.templates.<key>.text <string> (required)	# Text contains the Go text template format. The template references attributes from the data structure of the source secret. Refer to https://pkg.go.dev/text/template for more information.
spec.secrets.vaultStaticSecrets[].transformation.transformationRefs <[]object>	# TransformationRefs contain references to template configuration from SecretTransformation.
spec.secrets.vaultStaticSecrets[].transformation.transformationRefs[].ignoreExcludes <boolean>	# IgnoreExcludes controls whether to use the SecretTransformation's Excludes data key filters.
spec.secrets.vaultStaticSecrets[].transformation.transformationRefs[].ignoreIncludes <boolean>	# IgnoreIncludes controls whether to use the SecretTransformation's Includes data key filters.
spec.secrets.vaultStaticSecrets[].transformation.transformationRefs[].name <string> (required)	# Name of the SecretTransformation resource.
spec.secrets.vaultStaticSecrets[].transformation.transformationRefs[].namespace <string>	# Namespace of the SecretTransformation resource.
spec.secrets.vaultStaticSecrets[].transformation.transformationRefs[].templateRefs <[]object>	# TemplateRefs map to a Template found in this TransformationRef. If empty, then all templates from the SecretTransformation will be rendered to the K8s Secret.
spec.secrets.vaultStaticSecrets[].transformation.transformationRefs[].templateRefs[].keyOverride <string>	# KeyOverride to the rendered template in the Destination secret. If Key is empty, then the Key from reference spec will be used. Set this to override the Key set from the reference spec.
spec.secrets.vaultStaticSecrets[].transformation.transformationRefs[].templateRefs[].name <string> (required)	# Name of the Template in SecretTransformationSpec.Templates. the rendered secret data.
spec.secrets.vaultStaticSecrets[].type <string> (required) enum=kv-v1|kv-v2	# Type of the Vault static secret
spec.secrets.vaultStaticSecrets[].version <integer> min=0	# Version of the secret to fetch. Only valid for type kv-v2. Corresponds to version query parameter: https://developer.hashicorp.com/vault/api-docs/secret/kv/kv-v2#version
spec.syncConfig <object>	# SyncConfig provides configuration for syncing the secret data with the CSI driver.
spec.syncConfig.containerState <object> (required)	# ContainerState is the state of the container that the CSI driver always sync on. This configuration is useful to sync when the last state of the container is in the terminated state and the restart count is greater than 0.
spec.syncConfig.containerState.imagePattern <string>	# ImagePattern of the container. Can be expressed as a regular expression.
spec.syncConfig.containerState.namePattern <string>	# NamePattern of the container. Can be expressed as a regular expression.
spec.vaultAuthRef <object>	# VaultAuthRef is the reference to the VaultAuth resource.
spec.vaultAuthRef.name <string> (required)	# Name of the VaultAuth resource.
spec.vaultAuthRef.namespace <string>	# Namespace of the VaultAuth resource.
spec.vaultAuthRef.trustNamespace <boolean>	# TrustNamespace of the referring VaultAuth resource. This means that any Vault credentials will be provided by resources in the same namespace as the VaultAuth resource. Otherwise, the credentials will be provided by the secret resource's namespace.
status <object>	# CSISecretsStatus defines the observed state of CSISecrets
status.conditions <[]object>	# Conditions hold information that can be used by other apps to determine the health of the resource instance.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
